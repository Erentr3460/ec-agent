[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "cd packages/web && bun run server.ts"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
